
--before running the application open docker desktop and run redis:

docker run -d --name redis -p 6379:6379 redis:latest

login:
 user: admin@example.com
 password: password

Next steps: add employee caching with redis